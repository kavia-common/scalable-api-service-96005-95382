[build-system]
requires = ["setuptools>=65.0.0", "wheel"]

[project]
name = "grafana-integration-api"
version = "1.0.0"
description = "FastAPI service integrating with Grafana REST API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0,<1.0.0",
    "uvicorn[standard]>=0.30.0,<1.0.0",
    "httpx>=0.27.0,<1.0.0",
    "pydantic>=2.7.0,<3.0.0",
    "pydantic-settings>=2.3.0,<3.0.0",
    "pytest>=8.2.0,<9.0.0",
    "pytest-asyncio>=0.23.0,<1.0.0",
    "anyio>=4.3.0,<5.0.0",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["app"]
asyncio_mode = "auto"
